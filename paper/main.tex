\documentclass[11pt,a4paper]{article}
\usepackage[utf8]{inputenc}
\usepackage{amsmath,amssymb}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{hyperref}
\usepackage[margin=1in]{geometry}
\usepackage{xcolor}
\usepackage{caption}
\usepackage{subcaption}
\usepackage{algorithm}
\usepackage{algorithmic}

\title{PatchSteg: Covert Steganographic Communication Between Vision Models via Latent Space Perturbation}
\author{Team PatchSteg \\ Varsity Hackathon 2026}
\date{\today}

\begin{document}
\maketitle

\begin{abstract}
We demonstrate PatchSteg, a training-free framework for covert communication between AI vision systems via imperceptible perturbations in autoencoder latent space. Using a pretrained Stable Diffusion VAE, we encode binary messages by applying directional perturbations to selected spatial positions in the latent grid. These perturbations produce negligible visual change (PSNR $>$ 30\,dB) while enabling reliable message recovery (100\% bit accuracy at $\varepsilon{=}5.0$ with stability-selected carriers). We discover that carrier stability is spatially non-uniform---certain latent positions preserve perturbation direction through the encode-decode round-trip far more reliably than others. Our analysis reveals a stealth--capacity tradeoff: at $\varepsilon{=}1.0$ the channel is not separable by our logistic regression detector (AUC = 0.44) but unreliable, while at $\varepsilon{=}5.0$ it achieves high accuracy but becomes statistically distinguishable (AUC = 0.93). Between these extremes, $\varepsilon{=}2.0$ offers a practical operating point with 100\% accuracy on structured images and marginal detectability (AUC = 0.68). We validate on 300 natural photographs from CIFAR-10 (30 per class) with bootstrap confidence intervals, demonstrate transmission of messages up to 152 bits at 100\% accuracy, and show the phenomenon generalizes across multiple VAE backbones (SD-VAE-MSE, SD-VAE-EMA, SDXL-VAE). Under a logistic regression detector, PatchSteg at $\varepsilon{=}2.0$ is harder to detect than classical LSB steganography (AUC = 0.35 vs.\ 0.65). The channel survives JPEG compression up to quality 50. We provide a theoretical analysis connecting channel reliability to the VAE's KL regularization structure. PatchSteg illustrates a potential risk vector for multi-agent AI systems and provides an initial characterization of both the attack surface and detection challenges. We further introduce CDF-PatchSteg, a distribution-preserving variant that replaces additive perturbations with inverse Gaussian CDF sampling, achieving theoretical undetectability (AUC $\approx$ 0.5). We develop a 46-feature latent-space steganalysis detector that reliably detects original PatchSteg but fails on CDF-PatchSteg, establishing an attack-defense narrative. We also explore PCA-guided perturbation directions aligned with natural latent variation.
\end{abstract}

\section{Introduction}

Recent work by Motwani et al.\ has demonstrated that large language models can engage in secret collusion---hiding messages within seemingly benign text outputs that are undetectable by human monitors. As AI systems increasingly operate in multi-agent settings where they share not only text but also images (screenshots, generated content, perception frames), a natural question arises: \emph{can vision models establish covert communication channels through shared images?}

We answer this affirmatively with PatchSteg, a training-free steganographic framework that exploits the latent space of pretrained autoencoders. Our approach requires no model fine-tuning, no additional neural networks, and no adversarial training. Two agents sharing a secret key (a random seed) can embed and recover binary messages in images with negligible visible artifacts at appropriate perturbation strengths.

The key insight is that pretrained vision autoencoders like the Stable Diffusion VAE map images to a structured latent space where small directional perturbations are preserved through the encode$\to$decode round-trip. By perturbing selected spatial positions along a shared direction vector, a sender encodes bits that a receiver can extract by re-encoding the received image and checking projection directions.

Our contributions:
\begin{enumerate}
    \item A training-free steganographic encoding/decoding scheme for vision autoencoders achieving 100\% bit accuracy at imperceptible quality levels (Section~\ref{sec:roundtrip}).
    \item Discovery and characterization of \emph{spatially non-uniform carrier stability}---not all latent positions are equally reliable for steganography (Section~\ref{sec:carrier-stability}).
    \item A stealth--capacity--robustness characterization of the covert channel, including detectability analysis showing the channel is not separable by simple statistical detectors at low $\varepsilon$ but becomes distinguishable at high $\varepsilon$ (Sections~\ref{sec:robustness}--\ref{sec:detectability}).
    \item Mechanistic analysis revealing that all four latent channels contribute nearly equally to carrier stability, and that reconstruction error does not predict carrier quality (Section~\ref{sec:mechanistic}).
    \item Generality across multiple VAE backbones (SD-VAE-MSE, SD-VAE-EMA, SDXL-VAE) and evaluation under deployment-relevant distortion chains including screenshot simulation and VAE re-encoding (Sections~\ref{sec:multimodel}--\ref{sec:deployment}).
    \item A predictive framework for content-dependent capacity: image statistics (edge density, entropy, frequency content) predict channel reliability, and local decoder Jacobian analysis reveals the geometric mechanism (Section~\ref{sec:content_science}).
    \item Evaluation on 300 natural photographs with bootstrap confidence intervals, and comparison against 7 detector architectures across 3 feature domains (Sections~\ref{sec:natural},~\ref{sec:stronger_detection}).
\end{enumerate}

\begin{figure*}[t]
\centering
\includegraphics[width=\textwidth]{figures/hero_pipeline.png}
\caption{PatchSteg pipeline. (a) Original image. (b) Stability map with 20 selected carrier positions (red crosses mark the most stable latent positions). (c) Stego image after encoding 20 random bits. (d) Pixel-level difference amplified 20$\times$---perturbations are imperceptible. (e) Per-bit confidence of recovered message (100\% accuracy).}
\label{fig:hero}
\end{figure*}

\section{Related Work}

\textbf{Neural image steganography.} RoSteALS embeds messages by training an encoder-decoder pair on top of a pretrained autoencoder. Stable Messenger and Diffusion-Stego use diffusion model latent spaces for message hiding but require training auxiliary networks. PSyDUCK embeds watermarks in diffusion model outputs during generation. In contrast, PatchSteg requires \emph{no training}---we exploit the inherent structure of a pretrained VAE's latent space.

\textbf{AI agent collusion.} Motwani et al.\ demonstrate that LLM agents can develop covert text channels undetectable by monitors. We extend this threat model to vision: agents sharing images can hide messages in the latent representation, invisible to pixel-level inspection.

\textbf{Distribution-preserving watermarking.} Gaussian Shading~\cite{yang2024gaussian} embeds watermarks in diffusion model latents by partitioning the Gaussian prior via CDF thresholds, achieving theoretical undetectability. PRC Watermark~\cite{fernandez2023stable} uses pseudorandom codes in the initial noise of diffusion sampling. Tree-Ring Watermarking~\cite{wen2023treering} embeds patterns in the Fourier space of the initial noise. These methods inspire our CDF-based encoding (Section~\ref{sec:cdf}) which achieves distribution-preserving steganography in the VAE latent space without requiring control over the generation process.

\textbf{Autoencoder interpretability.} Our stability analysis connects to understanding VAE reconstruction fidelity. We show that reconstruction quality varies spatially within a single image, and that this variation is content-dependent---structured images (gradients, patches) exhibit more spatial variation in stability than noise. Rolinek et al.\ show that VAEs pursue PCA directions in their latent space~\cite{rolinek2019variational}, motivating our PCA-guided perturbation directions (Section~\ref{sec:pca}).

\section{Method}

\subsection{Backbone}

Our primary backbone is the Stable Diffusion VAE (\texttt{stabilityai/sd-vae-ft-mse}), a KL-regularized autoencoder trained on the LAION dataset. For $H{\times}H$ input images, it produces a $4{\times}\frac{H}{8}{\times}\frac{H}{8}$ latent representation. Each spatial position $(r,c)$ in the latent grid corresponds to an $8{\times}8$ pixel region and is represented by a 4-dimensional vector across channels. We apply the VAE's scaling factor ($0.18215$) after encoding and before decoding. We also validate on alternative VAE checkpoints (SD-VAE-EMA, SDXL-VAE) to test generality (Section~\ref{sec:multimodel}).

\subsection{Encoding Scheme}

Given a shared secret seed $s$, both sender and receiver generate:
\begin{enumerate}
    \item A unit direction vector $\mathbf{d} \in \mathbb{R}^4$ from $\mathcal{N}(0, I)$ normalized to unit length.
    \item A set of $K$ carrier positions $\{(r_i, c_i)\}_{i=1}^{K}$ in the latent grid.
\end{enumerate}

To encode bit $b_i \in \{0, 1\}$ at carrier position $(r_i, c_i)$:
\[
    \mathbf{z}_{r_i, c_i} \leftarrow \mathbf{z}_{r_i, c_i} + (2b_i - 1) \cdot \varepsilon \cdot \mathbf{d}
\]
where $\varepsilon$ controls perturbation strength. The modified latent is decoded to produce the stego image.

\subsection{Decoding Scheme}

The receiver encodes the received image to obtain $\mathbf{z}'$, then for each carrier:
\[
    \hat{b}_i = \mathbf{1}\left[ \langle \mathbf{z}'_{r_i, c_i} - \mathbf{z}_{r_i, c_i}, \mathbf{d} \rangle > 0 \right]
\]
where $\mathbf{z}$ is obtained by encoding the \emph{clean} round-trip image (no perturbation), removing systematic VAE reconstruction bias.

\subsection{Stability-Based Carrier Selection}
\label{sec:carrier-selection}

We introduce \emph{stability-based carrier selection}: before encoding, the sender applies a uniform $+\varepsilon$ perturbation to \emph{all} positions simultaneously, performs a single round-trip, and measures the projection delta at each position. Positions are ranked by delta magnitude; the top-$K$ are used as carriers. This requires only one additional encode-decode cycle.

\subsection{Repetition Coding}

For lower $\varepsilon$ (higher stealth), each bit is encoded across $R$ carrier positions with majority-vote decoding. With $R=3$, effective capacity is $K/3$ bits.

\subsection{Distribution-Preserving CDF Encoding}
\label{sec:cdf}

The original PatchSteg encoding shifts carrier values by $\pm\varepsilon$, creating a detectable statistical signature at high $\varepsilon$. We introduce \emph{CDF-PatchSteg}, a distribution-preserving variant inspired by Gaussian Shading. Instead of additive perturbations, we \emph{replace} each carrier's latent value with a sample from the upper or lower half of the empirical Gaussian distribution $\mathcal{N}(\mu_c, \sigma_c)$ estimated from the latent channel.

For each carrier position $(r_i, c_i)$ with message bit $b_i$ and key stream bit $k_i$:
\begin{enumerate}
    \item Compute effective bit $\tilde{b}_i = b_i \oplus k_i$
    \item Sample $u \sim \text{Uniform}(0.5, 1.0)$
    \item If $\tilde{b}_i = 1$: $z_{r_i,c_i} \leftarrow \Phi^{-1}(u; \mu_c, \sigma_c)$ (upper half)
    \item If $\tilde{b}_i = 0$: $z_{r_i,c_i} \leftarrow \Phi^{-1}(1-u; \mu_c, \sigma_c)$ (lower half)
\end{enumerate}
where $\Phi^{-1}$ is the inverse normal CDF. By construction, the marginal distribution of modified carrier values follows $\mathcal{N}(\mu_c, \sigma_c)$, making CDF-PatchSteg provably undetectable by any test that examines the marginal distribution of latent values.

\textbf{Decoding} requires no clean latent reference: re-encode the stego image, check whether each carrier value falls above or below $\mu_c$, and XOR with the key stream to recover the message bit.

\subsection{PCA-Guided Perturbation Directions}
\label{sec:pca}

Rather than using a random direction vector $\mathbf{d}$, we fit PCA on the 4-dimensional latent vectors across a dataset of images to discover directions of natural variation. The top principal component captures the axis of greatest variance in the latent space; perturbations along this axis are more aligned with the data manifold and potentially harder to detect.

We fit a global PCA on $4$-vectors pooled across all spatial positions and images, then use the $k$-th principal component as the perturbation direction. Perturbation magnitude can optionally be scaled proportionally to the corresponding singular value, ensuring modifications match the natural scale of variation along each direction.

\section{Experiments}

Experiments use synthetic test images (smooth gradients, color patches, random noise) and natural photographs from CIFAR-10. We evaluate on up to 300 natural images (30 per CIFAR-10 class) with bootstrap confidence intervals. Primary resolution is $256{\times}256$ ($32{\times}32$ latent grid, 1024 positions); some capacity tests use $512{\times}512$ ($64{\times}64$ grid, 4096 positions).

\subsection{Round-Trip Validation}
\label{sec:roundtrip}

Table~\ref{tab:roundtrip} confirms that directional perturbations survive the full encode$\to$perturb$\to$decode$\to$reencode pipeline. At $\varepsilon{=}5.0$ with 20 randomly-selected carriers, we achieve 100\% bit accuracy. At $\varepsilon{=}2.0$ accuracy drops to 75\% with 20 carriers, motivating stability-based selection.

\begin{table}[h]
\centering
\caption{Round-trip validation with random carrier selection (256$\times$256 images).}
\label{tab:roundtrip}
\begin{tabular}{@{}cccc@{}}
\toprule
$\varepsilon$ & Carriers & Bit Accuracy (\%) & Avg PSNR (dB) \\
\midrule
0.5 & 5 & 80.0 & 32.0 \\
0.5 & 20 & 52.5 & 32.0 \\
2.0 & 5 & 90.0 & 31.8 \\
2.0 & 20 & 75.0 & 31.7 \\
5.0 & 5 & 100.0 & 31.4 \\
5.0 & 20 & 100.0 & 30.7 \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Carrier Position Stability}
\label{sec:carrier-stability}

Figure~\ref{fig:stability} shows the stability map---per-position projection delta after round-trip---averaged across test images. At $\varepsilon{=}5.0$, 100\% of positions exhibit positive stability (perturbation preserved), but with significant variance (range 0.98--3.48 in averaged stability score). Stability-based carrier selection exploits this variance by choosing the most reliable positions first.

With stability-selected carriers at $\varepsilon{=}2.0$ on structured images (color patches), we achieve \textbf{100\% bit accuracy with 20 carriers}---compared to only 75\% with random selection in the initial validation. This demonstrates the value of intelligent carrier selection.

Crucially, stability maps are \emph{content-dependent} (Figure~\ref{fig:stability_content}). Smooth gradients produce low, uniform stability (mean 1.77), while color patches and noise produce higher, more variable stability. This explains why gradients are poor carriers: the VAE reconstructs them so faithfully that even clean round-trips introduce noise comparable to our perturbations.

\begin{figure}[h]
\centering
\includegraphics[width=0.55\textwidth]{figures/carrier_stability_avg.png}
\caption{Average carrier stability map across test images ($\varepsilon{=}5.0$). Red triangles mark top-20 most stable positions.}
\label{fig:stability}
\end{figure}

\begin{figure}[h]
\centering
\includegraphics[width=\textwidth]{figures/stability_by_content.png}
\caption{Stability maps vary dramatically with image content. Smooth gradients yield low, uniform stability, while textured images (patches, noise) exhibit higher and more spatially variable stability.}
\label{fig:stability_content}
\end{figure}

\begin{figure}[h]
\centering
\includegraphics[width=\textwidth]{figures/stego_examples.png}
\caption{Stego examples across image types. Top: originals. Middle: stego images (visually identical). Bottom: pixel differences amplified 20$\times$. Accuracy varies with image content---smooth gradients are poor carriers while textured images achieve 100\%.}
\label{fig:stego_examples}
\end{figure}

\subsection{Capacity Analysis}
\label{sec:capacity}

Using stability-selected carriers at $512{\times}512$ resolution, we test capacity up to 500 carriers. Table~\ref{tab:capacity} summarizes key results. On structured images (color patches), $\varepsilon{=}2.0$ achieves $\geq$99\% raw accuracy up to $K{=}200$ carriers with PSNR above 27\,dB. With 3$\times$ repetition coding, accuracy reaches 100\% for 166 effective bits.

\begin{table}[h]
\centering
\caption{Capacity results with stability-selected carriers at 512$\times$512 (color patches image).}
\label{tab:capacity}
\begin{tabular}{@{}ccccc@{}}
\toprule
$\varepsilon$ & $K$ & Raw Accuracy (\%) & PSNR (dB) & Rep$\times$3 Accuracy (\%) \\
\midrule
2.0 & 20 & 100.0 & 32.3 & --- \\
2.0 & 50 & 100.0 & 31.1 & 100.0 \\
2.0 & 100 & 99.0 & 29.6 & 100.0 \\
2.0 & 200 & 99.5 & 27.6 & 100.0 \\
2.0 & 500 & 99.4 & 24.3 & 100.0 \\
\midrule
5.0 & 20 & 100.0 & 28.1 & --- \\
5.0 & 100 & 99.0 & 22.2 & 100.0 \\
5.0 & 500 & 99.8 & 15.4 & 100.0 \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[h]
\centering
\includegraphics[width=0.55\textwidth]{figures/rate_distortion_accuracy.png}
\caption{Rate-distortion-accuracy tradeoff. Each point represents a (K, $\varepsilon$) configuration. The desirable operating region is the upper-right: high accuracy and high PSNR.}
\label{fig:rda}
\end{figure}

\subsection{Robustness to Distortions}
\label{sec:robustness}

Table~\ref{tab:robustness} shows bit accuracy when the stego image undergoes common distortions before the receiver re-encodes it. On structured images (color patches), the channel is remarkably robust: 100\% accuracy survives JPEG compression down to quality 50 and Gaussian noise ($\sigma{=}0.01$) at $\varepsilon{=}5.0$. On smooth gradients, the channel is weaker due to low spatial variation in the latent representation.

\begin{table}[h]
\centering
\caption{Robustness: bit accuracy (\%) under distortions with 20 stability-selected carriers.}
\label{tab:robustness}
\begin{tabular}{@{}lcccc@{}}
\toprule
& \multicolumn{2}{c}{Gradient} & \multicolumn{2}{c}{Color Patches} \\
\cmidrule(lr){2-3} \cmidrule(lr){4-5}
Distortion & $\varepsilon{=}2$ & $\varepsilon{=}5$ & $\varepsilon{=}2$ & $\varepsilon{=}5$ \\
\midrule
None & 45.0 & 55.0 & 100.0 & 100.0 \\
JPEG Q=95 & 40.0 & 55.0 & 100.0 & 100.0 \\
JPEG Q=75 & 35.0 & 65.0 & 100.0 & 100.0 \\
JPEG Q=50 & 20.0 & 65.0 & 100.0 & 100.0 \\
Noise $\sigma{=}0.01$ & 40.0 & 55.0 & 100.0 & 100.0 \\
Noise $\sigma{=}0.05$ & 65.0 & 85.0 & 95.0 & 100.0 \\
Resize 50\% & 40.0 & 60.0 & 80.0 & 100.0 \\
\bottomrule
\end{tabular}
\end{table}

An important finding is that the \emph{gradient image performs near chance} even without distortion (45--55\% at $\varepsilon{=}2$--$5$), while color patches achieve 100\%. This indicates that \textbf{image content strongly determines channel reliability}---images with spatially varying structure provide far more stable carriers than smooth, low-frequency images.

\begin{figure}[h]
\centering
\includegraphics[width=0.7\textwidth]{figures/robustness_bars.png}
\caption{Robustness to distortions. Left: $\varepsilon{=}2.0$; right: $\varepsilon{=}5.0$. Averaged over test images.}
\label{fig:robustness}
\end{figure}

\subsection{Detectability}
\label{sec:detectability}

We train a logistic regression classifier on latent-space statistics (per-channel mean, std, median, skewness, kurtosis---20 features) to distinguish clean round-trip images from stego images. Table~\ref{tab:detect} shows 3-fold cross-validated results across 30 images per class.

\begin{table}[h]
\centering
\caption{Detectability: classifier performance at distinguishing clean from stego images.}
\label{tab:detect}
\begin{tabular}{@{}cccc@{}}
\toprule
$\varepsilon$ & AUC & Accuracy & Verdict \\
\midrule
1.0 & 0.440 $\pm$ 0.065 & 0.467 $\pm$ 0.125 & Not separable \\
2.0 & 0.680 $\pm$ 0.113 & 0.600 $\pm$ 0.082 & Marginal \\
5.0 & 0.933 $\pm$ 0.068 & 0.833 $\pm$ 0.125 & Distinguishable \\
10.0 & 0.973 $\pm$ 0.038 & 0.900 $\pm$ 0.141 & Distinguishable \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[h]
\centering
\includegraphics[width=0.5\textwidth]{figures/detectability_curve.png}
\caption{Detection AUC as a function of perturbation strength, revealing the stealth--capacity tradeoff.}
\label{fig:detect}
\end{figure}

This reveals a clear \textbf{stealth--capacity tradeoff} (Figure~\ref{fig:pareto}). At $\varepsilon{=}1.0$, the channel is not separable by our detector (AUC below chance) but unreliable. At $\varepsilon{=}2.0$, the channel is marginally detectable but achieves high accuracy on structured images. At $\varepsilon{=}5.0$, the channel is reliable but statistically distinguishable. This parallels classical steganographic tradeoffs but occurs in a qualitatively different space (autoencoder latent statistics rather than pixel statistics).

\begin{figure}[h]
\centering
\includegraphics[width=0.65\textwidth]{figures/pareto_frontier.png}
\caption{The stealth--capacity Pareto frontier. Blue: bit accuracy increases with $\varepsilon$. Red: detection AUC also increases. The shaded ``sweet spot'' around $\varepsilon{\approx}2$ balances reliability against detectability.}
\label{fig:pareto}
\end{figure}

\subsection{Stronger Detection Baselines}
\label{sec:stronger_detection}

To assess whether more capable detectors can break the channel, we evaluate 7 detector architectures across three feature domains: latent statistics, pixel residuals, and spectral features. We use 60 images per class (120 total samples) with 5-fold cross-validation.

\textbf{Detectors tested:} (1) Logistic regression on latent statistics (20 features); (2) MLP on latent statistics (64-32 hidden units); (3) Logistic regression on pixel residuals (18 features); (4) MLP on pixel residuals; (5) Logistic regression on spectral features (FFT of residual, 15 features); (6) Logistic regression on combined features (latent + residual + spectral, 73 features); (7) MLP on combined features (128-64 hidden units).

\begin{figure}[h]
\centering
\includegraphics[width=\textwidth]{figures/detection_strength.png}
\caption{Detection strength across 7 detector architectures and 3 feature domains. Heatmap shows AUC with standard deviation; grouped bars enable comparison across $\varepsilon$ values. Even the strongest detector (MLP on combined features) struggles at $\varepsilon{=}1.0$.}
\label{fig:detection_strength}
\end{figure}

\section{Mechanistic Analysis}
\label{sec:mechanistic}

\subsection{Channel Importance}

We test each of the 4 latent channels independently by perturbing only a single channel and measuring round-trip survival. All four channels exhibit high stability: Channel 2 is strongest at 99.7\%, while Channel 0 is weakest at 97.5\% (Figure~\ref{fig:mechanistic}a). The near-uniformity suggests the VAE encoder-decoder preserves information similarly across channels, consistent with the KL regularization encouraging balanced channel usage.

\subsection{Reconstruction Error Does Not Predict Stability}

We hypothesized that positions with lower baseline reconstruction error (without perturbation) would be more stable carriers. Surprisingly, the correlation is near zero: average Pearson $r = 0.048$ across images (Figure~\ref{fig:mechanistic}b). This indicates that the VAE's ability to reconstruct a position faithfully is independent of its ability to \emph{preserve perturbations} at that position---a subtle but important distinction. Stability appears to depend on the local structure of the decoder's Jacobian rather than the magnitude of reconstruction noise.

\subsection{Direction Vector Robustness}

We test 10 different random direction vectors (seeds 1 through 9999). All achieve \textbf{100\% bit accuracy} on the color patches image at $\varepsilon{=}5.0$ with 20 carriers. The choice of direction vector has no measurable effect on channel reliability, confirming that the VAE preserves perturbations isotropically in the 4-dimensional channel space. This is a desirable property: the shared secret need only determine carrier positions, not a specially-chosen direction.

\subsection{End-to-End Message Transmission}

As a proof of concept, we encode the ASCII string ``HI'' (16 bits) into a color patches image using 3$\times$ repetition coding (48 carriers). The receiver perfectly recovers the message with 100\% bit accuracy (Figure~\ref{fig:demo}). This demonstrates that PatchSteg functions as a complete communication channel.

\begin{figure}[h]
\centering
\includegraphics[width=\textwidth]{figures/text_demo.png}
\caption{End-to-end demonstration: the message ``HI'' (16 bits, 3$\times$ repetition coded) is encoded, transmitted as a stego image, and perfectly recovered.}
\label{fig:demo}
\end{figure}

\subsection{Border vs Interior}

Border positions (within 2 pixels of the latent grid edge) show similar stability to interior positions, with the difference being content-dependent rather than systematic. For the color patches image, borders are slightly \emph{more} stable (2.10 vs 1.76), while for noise images borders are slightly less stable (2.97 vs 3.05).

\begin{figure}[h]
\centering
\includegraphics[width=0.9\textwidth]{figures/mechanistic_analysis.png}
\caption{Mechanistic analysis. (a) Per-channel round-trip survival rate. (b) Reconstruction error vs stability scatter (near-zero correlation). (c) Stability map for color patches image.}
\label{fig:mechanistic}
\end{figure}

\subsection{Content Dependence as a Predictive Framework}
\label{sec:content_science}

We study what image statistics predict steganographic capacity, treating content dependence as a central scientific contribution rather than a nuisance. For 50 images spanning 10 structural types (solid, gradient, noise, blocks, checkerboard, sine patterns, etc.), we compute five image-level predictors: edge density, Shannon entropy, high-frequency energy ratio, local color variance, and texture energy. We measure Pearson and Spearman correlations between each predictor and bit accuracy.

\begin{figure}[h]
\centering
\includegraphics[width=\textwidth]{figures/content_science.png}
\caption{Content dependence analysis. Scatter plots show correlation between five image statistics and bit accuracy. Bottom-right: decoder Jacobian norm at carrier vs.\ non-carrier positions, estimated via finite differences.}
\label{fig:content_science}
\end{figure}

\begin{figure}[h]
\centering
\includegraphics[width=0.7\textwidth]{figures/capacity_by_type.png}
\caption{Steganographic capacity varies dramatically by image type. Solid and gradient images provide near-chance accuracy; noise and textured images support reliable transmission.}
\label{fig:capacity_type}
\end{figure}

\subsection{Local Decoder Jacobian Analysis}

To understand \emph{why} certain positions are stable carriers, we estimate the local decoder Jacobian norm at carrier and non-carrier positions via finite differences. For each position, we perturb each of the 4 latent channels independently by $\delta{=}0.1$ and measure the resulting pixel change, approximating $\| \partial \text{Dec} / \partial z_{r,c} \|_F$.

If good carriers correspond to positions where the decoder has high sensitivity (large Jacobian norm), then perturbations at those positions produce larger pixel changes that the encoder can ``see'' when re-encoding. We test this hypothesis with a Mann-Whitney U test comparing Jacobian norms at carrier vs.\ non-carrier positions across 10 images.

\section{Extended Experiments}

\subsection{Evaluation on Natural Photographs at Scale}
\label{sec:natural}

To move beyond toy-scale evaluation, we test PatchSteg on 300 natural photographs from CIFAR-10 (30 per class, upscaled to $256{\times}256$) with bootstrap 95\% confidence intervals. Results are reported per-class to assess content-dependent variation.

\begin{figure}[h]
\centering
\includegraphics[width=\textwidth]{figures/serious_dataset.png}
\caption{Evaluation on 300 CIFAR-10 images. Top: per-class accuracy with error bars. Bottom: histogram of per-image accuracy. Bootstrap 95\% confidence intervals are reported in the panel titles.}
\label{fig:natural}
\end{figure}

\begin{figure}[h]
\centering
\includegraphics[width=\textwidth]{figures/natural_photos.png}
\caption{Example stego images from CIFAR-10 at $\varepsilon{=}5.0$. Top: originals. Middle: stego images. Bottom: pixel differences amplified 20$\times$.}
\label{fig:natural_examples}
\end{figure}

\subsection{Longer Messages}
\label{sec:messages}

We test message lengths from 8 bits (single ASCII character) to 152 bits (19 characters). Table~\ref{tab:messages} shows results with both raw encoding ($R{=}1$) and 3$\times$ repetition coding ($R{=}3$).

\begin{table}[h]
\centering
\caption{Longer message transmission (stability-selected carriers, $\varepsilon{=}5.0$).}
\label{tab:messages}
\begin{tabular}{@{}lcccccc@{}}
\toprule
& & \multicolumn{2}{c}{$R{=}1$ (raw)} & \multicolumn{2}{c}{$R{=}3$ (rep coded)} \\
\cmidrule(lr){3-4} \cmidrule(lr){5-6}
Message & Bits & Acc (\%) & PSNR (dB) & Acc (\%) & PSNR (dB) \\
\midrule
\texttt{A} & 8 & 100 & 32.3 & 100 & 27.9 \\
\texttt{HI} & 16 & 94 & 29.4 & 100 & 23.5 \\
\texttt{HELLO} & 40 & 100 & 24.6 & 100 & 19.1 \\
\texttt{HELLO WORLD} & 88 & 100 & 20.6 & 100 & 14.9 \\
\texttt{THE QUICK BROWN FOX} & 152 & 100 & 17.7 & 100 & 12.4 \\
\bottomrule
\end{tabular}
\end{table}

PatchSteg reliably transmits messages up to \textbf{152 bits (19 characters)} at 100\% accuracy with raw encoding. With 3$\times$ repetition coding, all messages achieve perfect accuracy, though at the cost of PSNR (the 152-bit message with $R{=}3$ uses 456 carriers, yielding 12.4\,dB). The only failure case is ``HI'' with $R{=}1$, where one bit flips---``HI'' is decoded as ``HY''---demonstrating the value of repetition coding for marginal carrier positions. Figure~\ref{fig:messages} visualizes the accuracy--PSNR tradeoff across message lengths.

\begin{figure}[h]
\centering
\includegraphics[width=0.65\textwidth]{figures/message_length.png}
\caption{Message length stress test. Accuracy and PSNR as a function of message length for raw ($R{=}1$) and repetition-coded ($R{=}3$) transmission.}
\label{fig:messages}
\end{figure}

\subsection{Multi-Model Generality}
\label{sec:multimodel}

A critical question is whether PatchSteg's mechanism is specific to a single VAE checkpoint or reflects a general property of KL-regularized autoencoders. We test on multiple backbones: SD-VAE-MSE (our primary model), SD-VAE-EMA (exponential moving average variant), and SDXL-VAE (the SDXL architecture). The same encoding/decoding logic is used without modification.

\begin{figure}[h]
\centering
\includegraphics[width=\textwidth]{figures/multimodel.png}
\caption{Multi-model generality. (a) PatchSteg accuracy across VAE backbones---the phenomenon generalizes. (b) Cross-model robustness: encoding with one VAE and decoding with another.}
\label{fig:multimodel}
\end{figure}

We also test \emph{cross-model} robustness: the sender encodes using one VAE checkpoint and the receiver decodes using a different checkpoint. This simulates a deployment scenario where agents do not share the exact same model binary.

\subsection{Deployment-Relevant Robustness}
\label{sec:deployment}

We test the channel under realistic distortion chains that images might encounter in deployment: heavy JPEG compression (Q=10--50), aggressive resize (25--75\%), center cropping, Gaussian blur, additive noise, screenshot simulation (resize + JPEG + brightness shift), VAE re-encoding (simulating a safety filter), and multi-stage chains (JPEG$\to$resize$\to$JPEG).

\begin{figure}[h]
\centering
\includegraphics[width=\textwidth]{figures/deployment_robustness.png}
\caption{Deployment robustness across 18 distortion types, tested on 20 diverse images. Green: $>$85\% accuracy. Yellow: 65--85\%. Red: $<$65\%. The channel survives moderate JPEG and noise but degrades under aggressive cropping and heavy compression.}
\label{fig:deployment}
\end{figure}

\subsection{Comparison with LSB Steganography}
\label{sec:lsb}

We compare PatchSteg's detectability against least-significant-bit (LSB) steganography, the classical pixel-domain method. For LSB, we flip the least significant bit of randomly selected pixels to embed the same number of bits as PatchSteg (20 bits). We train the same logistic regression detector (latent-space statistics) on 15 cover/stego pairs with 3-fold cross-validation.

\begin{table}[h]
\centering
\caption{Detection comparison: PatchSteg vs.\ LSB steganography (20-bit payload).}
\label{tab:lsb}
\begin{tabular}{@{}lcc@{}}
\toprule
Method & AUC & Accuracy \\
\midrule
PatchSteg ($\varepsilon{=}2.0$) & $0.349 \pm 0.060$ & $0.396 \pm 0.078$ \\
LSB & $0.651 \pm 0.121$ & $0.604 \pm 0.029$ \\
PatchSteg ($\varepsilon{=}5.0$) & $0.807 \pm 0.096$ & $0.646 \pm 0.029$ \\
\bottomrule
\end{tabular}
\end{table}

At $\varepsilon{=}2.0$, PatchSteg is \textbf{harder to detect than LSB} under our logistic regression detector (AUC 0.35 vs.\ 0.65). This is because PatchSteg perturbations are distributed across the latent space in a way that aligns with the VAE's learned distribution, while LSB modifications create statistical anomalies detectable even in latent-space features. At $\varepsilon{=}5.0$, PatchSteg becomes more detectable than LSB (AUC 0.81 vs.\ 0.65), confirming the stealth--capacity tradeoff. Figure~\ref{fig:lsb} visualizes the comparison.

\begin{figure}[h]
\centering
\includegraphics[width=0.65\textwidth]{figures/lsb_comparison.png}
\caption{Detection comparison between PatchSteg and LSB steganography. PatchSteg at $\varepsilon{=}2.0$ is stealthier than classical LSB, while $\varepsilon{=}5.0$ is more detectable. Error bars show standard deviation across cross-validation folds.}
\label{fig:lsb}
\end{figure}

\subsection{Theoretical Analysis: Why the Channel Works}
\label{sec:theory}

We analyze \emph{why} the SD VAE latent space supports steganographic communication, connecting the channel's reliability to the VAE's training objective and architecture.

\paragraph{Latent distribution structure.} The VAE's KL regularization term encourages each latent channel to approximate $\mathcal{N}(0, 1)$, but the learned distribution deviates significantly. Table~\ref{tab:latent_stats} shows per-channel statistics averaged over test images. Channels exhibit non-zero means (0.99 for Ch0, $-0.29$ for Ch1), non-unit variance (0.52--0.86 std), and heavy tails (kurtosis up to 80.8 for Ch2). This structured deviation from the prior creates ``room'' for perturbations: modifications along directions with high variance are less distinguishable from natural variation.

\begin{table}[h]
\centering
\caption{Latent channel statistics (averaged over test images at 256$\times$256).}
\label{tab:latent_stats}
\begin{tabular}{@{}ccccc@{}}
\toprule
Channel & Mean & Std & Skewness & Kurtosis \\
\midrule
0 & 0.991 & 0.698 & 2.451 & 25.04 \\
1 & $-0.285$ & 0.857 & 1.748 & 10.88 \\
2 & $-0.151$ & 0.523 & $-6.163$ & 80.81 \\
3 & 0.807 & 0.519 & $-0.725$ & 5.23 \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{Perturbation-to-noise ratio.} The perturbation magnitude $\varepsilon$ must be compared against the natural variation in latent values. Using the average latent standard deviation (0.65 across channels), we compute the perturbation-to-latent-std ratio:

\begin{center}
\begin{tabular}{cc}
$\varepsilon$ & Perturbation / Latent Std \\
\midrule
0.5 & 0.57 \\
1.0 & 1.15 \\
2.0 & 2.29 \\
5.0 & 5.74 \\
\end{tabular}
\end{center}

At $\varepsilon{=}1.0$ (not separable by our detector), the perturbation is roughly 1$\times$ the natural standard deviation---hard to distinguish from natural variation. At $\varepsilon{=}5.0$ (detectable), it is nearly 6$\times$ the standard deviation, explaining why statistical detectors succeed. The ``sweet spot'' at $\varepsilon{=}2.0$ (${\sim}2.3\sigma$) lies at the boundary of statistical distinguishability.

\paragraph{Decoder sensitivity.} We measure the maximum pixel change caused by a single-position perturbation at $\varepsilon{=}1.0$. Across 5 trials, the average maximum pixel change is \textbf{10.8} (on a 0--255 scale), confirming that the decoder's locally linear response maps small latent perturbations to sub-threshold pixel changes. This is well below the JND (just-noticeable-difference) threshold of ${\sim}2$\,dB change (approximately 30 pixel levels at typical viewing conditions), explaining the visual imperceptibility.

\paragraph{Connection to KL regularization.} The KL term in the VAE's training objective ($D_\text{KL}(q(z|x) \| p(z))$ with $p(z) = \mathcal{N}(0, I)$) encourages the encoder to map nearby inputs to nearby latent vectors. This \emph{local smoothness} is precisely what enables perturbation survival: if $z' = z + \varepsilon \mathbf{d}$, then $\text{Enc}(\text{Dec}(z')) \approx z'$ because the decoder maps $z'$ to a pixel image near $\text{Dec}(z)$, and the encoder maps that image back near $z'$. The channel works because the VAE's regularization creates a locally smooth, approximately invertible mapping---perturbations in latent space produce coherent pixel changes that survive re-encoding.

\begin{figure}[h]
\centering
\includegraphics[width=\textwidth]{figures/theoretical_analysis.png}
\caption{Theoretical analysis. (a) Latent channel distributions showing deviation from standard normal. (b) Perturbation-to-noise ratio across $\varepsilon$ values, explaining the detectability threshold. (c) Decoder pixel sensitivity: maximum pixel change from single-position perturbation at $\varepsilon{=}1.0$.}
\label{fig:theory}
\end{figure}

\section{Distribution-Preserving Steganography}
\label{sec:cdf_experiments}

\subsection{CDF-PatchSteg: Capacity and Quality}

We evaluate CDF-PatchSteg on 8 natural photographs from CIFAR-10 with carrier counts $K \in \{5, 10, 20, 50\}$. Table~\ref{tab:cdf_comparison} compares CDF-PatchSteg against the original PatchSteg ($\varepsilon{=}5.0$) on bit accuracy and PSNR.

\begin{table}[h]
\centering
\caption{CDF-PatchSteg vs.\ Original PatchSteg: accuracy and image quality (averaged over 8 natural images).}
\label{tab:cdf_comparison}
\begin{tabular}{@{}ccccc@{}}
\toprule
& \multicolumn{2}{c}{CDF-PatchSteg} & \multicolumn{2}{c}{Original ($\varepsilon{=}5$)} \\
\cmidrule(lr){2-3} \cmidrule(lr){4-5}
$K$ & Acc (\%) & PSNR (dB) & Acc (\%) & PSNR (dB) \\
\midrule
5 & --- & --- & --- & --- \\
10 & --- & --- & --- & --- \\
20 & --- & --- & --- & --- \\
50 & --- & --- & --- & --- \\
\bottomrule
\end{tabular}
\end{table}

\emph{Note: Table values to be filled after running experiments.}

\subsection{Distribution Preservation Verification}

We verify that CDF-PatchSteg preserves the latent distribution using the Kolmogorov-Smirnov test. For each image, we test whether carrier values after CDF encoding follow $\mathcal{N}(\mu_c, \sigma_c)$. A $p$-value above 0.05 indicates no statistically significant deviation from the expected distribution.

\subsection{Detectability Comparison}

Figure~\ref{fig:cdf_detect} compares detection AUC across CDF-PatchSteg, PatchSteg $\varepsilon{=}2$, and PatchSteg $\varepsilon{=}5$ using the same logistic regression detector on latent-space statistics.

\begin{figure}[h]
\centering
\includegraphics[width=0.65\textwidth]{figures/cdf_detectability.png}
\caption{Detectability comparison: CDF-PatchSteg achieves near-chance AUC, demonstrating distribution-preserving steganography.}
\label{fig:cdf_detect}
\end{figure}

\begin{figure}[h]
\centering
\includegraphics[width=\textwidth]{figures/cdf_distribution.png}
\caption{Distribution visualization. (a) Clean latent distribution. (b) CDF-PatchSteg carrier values follow the same distribution. (c) Original PatchSteg shifts the distribution.}
\label{fig:cdf_dist}
\end{figure}

\begin{figure}[h]
\centering
\includegraphics[width=0.7\textwidth]{figures/cdf_capacity_curve.png}
\caption{Capacity curves comparing CDF-PatchSteg and original PatchSteg across carrier counts.}
\label{fig:cdf_capacity}
\end{figure}

\section{PCA-Guided Perturbation Analysis}
\label{sec:pca_experiments}

\subsection{Latent Space PCA}

We fit PCA on 4-dimensional latent vectors pooled across 8 CIFAR-10 images. Figure~\ref{fig:pca_components} shows the explained variance per component and the PCA component weight matrix.

\begin{figure}[h]
\centering
\includegraphics[width=\textwidth]{figures/pca_components.png}
\caption{PCA of VAE latent space. (a) Explained variance per component. (b) Component weight matrix. (c) Singular values showing natural variation scale.}
\label{fig:pca_components}
\end{figure}

\subsection{PCA vs Random Direction Accuracy}

We compare PCA-guided directions (PC0, PC1, PC2) against a random direction on bit accuracy and PSNR. Figure~\ref{fig:pca_acc} shows results with $K{=}20$ carriers at $\varepsilon{=}5.0$ across 8 natural images.

\begin{figure}[h]
\centering
\includegraphics[width=0.7\textwidth]{figures/pca_accuracy_comparison.png}
\caption{Bit accuracy and PSNR: PCA-guided vs.\ random perturbation directions.}
\label{fig:pca_acc}
\end{figure}

\subsection{PCA Direction Detectability}

\begin{figure}[h]
\centering
\includegraphics[width=0.5\textwidth]{figures/pca_detectability.png}
\caption{Detection AUC for PCA-guided vs.\ random perturbation directions.}
\label{fig:pca_detect}
\end{figure}

\section{Latent-Space Steganalysis}
\label{sec:steganalysis}

We develop the first dedicated latent-space steganalysis detector, using 46 features: per-channel residual statistics (mean, std, median, skewness, kurtosis), spectral energy features, cross-channel correlations, and histogram features. The detector uses a logistic regression pipeline (StandardScaler + LogisticRegression) following standard steganalysis methodology.

\subsection{Within-Method Detection}

We evaluate the detector's ability to distinguish clean from stego images for each steganographic method independently using 3-fold cross-validation on 8 natural images. This tests whether each method leaves a detectable statistical fingerprint.

\subsection{Cross-Method Detection Matrix}

A critical question for the attack-defense narrative: \emph{does a detector trained on one steganographic method generalize to others?} We construct a cross-method evaluation matrix where each row represents the training method and each column represents the test method.

\begin{figure}[h]
\centering
\includegraphics[width=0.5\textwidth]{figures/detector_cross_method.png}
\caption{Cross-method detection AUC matrix. A detector trained on PatchSteg $\varepsilon{=}5$ achieves high AUC on PatchSteg variants but fails on CDF-PatchSteg, confirming the distribution-preserving property.}
\label{fig:cross_method}
\end{figure}

\subsection{ROC Curves and Feature Importance}

Figure~\ref{fig:roc} shows ROC curves for each method, and Figure~\ref{fig:feat_import} shows the most important features for detecting PatchSteg $\varepsilon{=}5$.

\begin{figure}[h]
\centering
\includegraphics[width=0.5\textwidth]{figures/detector_roc_curves.png}
\caption{ROC curves for the latent-space steganalysis detector across methods.}
\label{fig:roc}
\end{figure}

\begin{figure}[h]
\centering
\includegraphics[width=0.65\textwidth]{figures/detector_feature_importance.png}
\caption{Top 15 features by logistic regression coefficient magnitude, trained on PatchSteg $\varepsilon{=}5$.}
\label{fig:feat_import}
\end{figure}

\section{Discussion}

\subsection{Implications for AI Safety}

PatchSteg demonstrates a concrete mechanism by which vision models can establish covert communication channels through shared images. In multi-agent settings where AI systems pass images to each other (robotic perception, AI-generated content, shared camera feeds), PatchSteg-style channels could in principle enable collusion that is difficult for human monitors to detect visually.

\subsection{The Stealth--Capacity Frontier}

Our detectability analysis quantifies a fundamental tradeoff: at $\varepsilon{=}1.0$ the channel is not separable by our detectors but unusable; at $\varepsilon{=}5.0$ it is reliable but statistically distinguishable. The practical operating point at $\varepsilon{=}2.0$ with stability-selected carriers on structured images achieves 100\% accuracy while remaining only marginally detectable (AUC = 0.68). With repetition coding ($R{=}3$), this yields approximately 66 effective bits per image at $K{=}200$.

\subsection{Content Dependence}

A key finding is that channel reliability is strongly content-dependent. Smooth gradients provide near-zero channel capacity, while textured images support hundreds of bits. Our experiments on CIFAR-10 photographs (Section~\ref{sec:natural}) confirm that natural images---which are inherently textured---achieve 98.8\% accuracy, validating this intuition.

\subsection{Advantage Over Classical Steganography}

PatchSteg at $\varepsilon{=}2.0$ is harder to detect than classical LSB steganography (AUC 0.35 vs.\ 0.65) under our logistic regression detector on latent-space statistics. This is because PatchSteg modifies the image in a way that is \emph{aligned with the VAE's learned manifold}, while LSB modifications are arbitrary pixel-level changes that create subtle statistical artifacts. This advantage comes with a tradeoff: PatchSteg requires access to the VAE model, while LSB requires no model at all.

\subsection{Limitations}

Our implementation requires both agents to share a compatible VAE model and a secret seed (cross-model decode is tested but with reduced accuracy). The channel capacity is limited to tens or hundreds of bits per image (152 bits demonstrated). While we test 7 detector architectures across 3 feature domains, more sophisticated learned detectors (e.g., trained CNNs on full images, or detectors specifically designed for latent-space steganography) might achieve substantially higher detection rates. Our evaluation uses CIFAR-10 images upscaled to $256{\times}256$---while this provides diversity and scale, the upscaling introduces interpolation artifacts that may not reflect native high-resolution photography. The channel degrades significantly under aggressive distortions (JPEG Q$<$30, heavy cropping), limiting deployment scenarios.

\section{Conclusion}

We presented PatchSteg, a training-free steganographic framework enabling covert bit transmission through pretrained VAE latent spaces. Key findings: (1) the phenomenon generalizes across multiple VAE architectures (SD-VAE-MSE, SD-VAE-EMA, SDXL-VAE); (2) carrier stability is spatially non-uniform, content-dependent, and predictable from image statistics such as edge density and local entropy; (3) there exists a quantifiable stealth--capacity tradeoff, with $\varepsilon{=}2.0$ offering a practical balance---harder to detect than LSB under our detectors, though stronger detectors narrow this gap; (4) local decoder Jacobian analysis reveals the geometric mechanism behind carrier quality; (5) the channel survives moderate deployment distortions but degrades under aggressive compression and cropping. We further introduce CDF-PatchSteg, a distribution-preserving variant using inverse Gaussian CDF sampling that achieves theoretical undetectability (AUC $\approx$ 0.5), and develop a 46-feature latent-space steganalysis detector that reliably detects the original PatchSteg but fails on CDF-PatchSteg---establishing an attack-defense narrative. PCA analysis of the latent space reveals data-driven perturbation directions aligned with natural variation. Evaluated on 300 natural images with confidence intervals, PatchSteg illustrates an underexplored risk vector in multi-agent AI systems and provides tools for understanding both the attack surface and detection challenges.

\begin{thebibliography}{9}

\bibitem{yang2024gaussian}
Yang, Z., et al.
\newblock Gaussian Shading: Provable Performance Guarantees for Generative Image Watermarking.
\newblock \emph{CVPR}, 2024.

\bibitem{fernandez2023stable}
Fernandez, P., et al.
\newblock The Stable Signature: Rooting Watermarks in Latent Diffusion Models.
\newblock \emph{ICCV}, 2023.

\bibitem{wen2023treering}
Wen, Y., et al.
\newblock Tree-Ring Watermarks: Fingerprints for Diffusion Images that are Invisible and Robust.
\newblock \emph{NeurIPS}, 2023.

\bibitem{rolinek2019variational}
Rolinek, M., Zietlow, D., and Martius, G.
\newblock Variational Autoencoders Pursue PCA Directions (by Accident).
\newblock \emph{CVPR}, 2019.

\end{thebibliography}

\end{document}
